<!DOCTYPE html>
<html lang="en">
	<head>
	    <!-- Required meta tags -->
	    <meta charset="utf-8">
	    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
		<link rel="icon" type="image/png" href="{{ base_url() }}/img/favicon.ico">

	   <!-- Bootstrap CSS -->
	   <link rel="stylesheet" type="text/css" href="{{ base_url() }}/css/bootstrap.min.css">

		<!-- Mis estilos css -->
		<link rel="stylesheet" type="text/css" href="{{ base_url() }}/css/estilosPrint.css">
		<title>{{ titulo }}</title>
		<style type="text/css">
			body {
				font-size: 100%;
			}
			.cellRight {
			  text-align: right;
			}
			p#razon {
				font-size: 1.1em;
			}
			p.data {
				/*font-size: 0.875em;*/
				margin: 1px;
			}
			hr.lin {
				margin: 10px auto;
			}
			#btnImprimir {
				margin-right: 20px;
			}
			textarea {
				width: 800px;
				height: 300px;
				margin-bottom: 20px;
			}
			textarea:disabled {
				color: black;
			}
		</style>
	</head>
	<body>
		<header>
			<div class="container-fluid">
				<div class="row justify-content-end">
					<div class="col-xs-3">
						<button id="btnImprimir" type="button" class="btn btn-primary">Imprimir</button>
						<button id="btnVolver" type="button" class="btn btn-light mr-1">Volver</button>
					</div>
				</div>
				<div class="row">
					<div class="col">
						<hr>
					</div>
				</div>
			</div>
		</header>

		<div class="container-fluid">
			<div class="row">
				<div class="col mt-4">
					<h2 class="text-center"><strong>PRESUPUESTO</strong></h2>
				</div>
			</div> {# end row #}

			<div class="row">
				<div class="col-sm-10 offset-sm-1">
					<hr class="mt-0">
				</div>
			</div>
			<div class="row mb-0">
				<div class="col-sm-8 offset-sm-1 pl-4">
					<p id="razon">Nombre: <strong>{{ presupuesto.nombre }} ({{ presupuesto.cliente_id }})</strong></p>
				</div>
				<div class="col-sm-2 pr-4">
					<p class="text-right">Fecha emisión:&nbsp;{{ presupuesto.fecha|date('d/m/Y') }}</p>
					<p class="text-right">Número:&nbsp;{{ presupuesto.id }}</p>
				</div>
			</div>
			<div class="row">
				<div class="col-sm-4 offset-sm-1 pl-4">
					<p class="data">Cuit/DNI:&nbsp; {{ presupuesto.cuitdni }}</p>
					<p class="data">Dirección:&nbsp; {{ presupuesto.domicilio }}</p>
				</div>
				<div class="col-sm-3">
					<p class="data">Localidad:&nbsp; {{ presupuesto.localidad }}</p>
				</div>
				<div class="col-3">
					<p class="data">Teléfono:&nbsp; {{ presupuesto.telefono }} - Celular:&nbsp; {{ presupuesto.celular }}</p>
					<p class="data">Provincia:&nbsp; {{ presupuesto.provincia }}</p>
				</div>
			</div>
			<div class="row">
				<div class="col-sm-10 offset-sm-1">
					<hr class="lin">
				</div>
			</div>

			<div class="row mt-2">
				<div class="col-sm-10 offset-sm-1 pl-4 pr-4">
					<p class="mb-1">Detalle:</p>
					<textarea disabled="true">{{ presupuesto.detalle }}</textarea>
				</div>
			</div>

			<div class="row">
				<div class="col-6"></div>
				<div class="col-5">
					<p class="text-right font-weight-bold pr-4">Importe $ {{ presupuesto.importe|number_format(2, ',', '.') }}</p>
				</div>
			</div>

		</div>  {# end container #}

		<script src="{{ base_url() }}/js/jquery-3.5.1.min.js"></script>
		<script type="text/javascript">
			// Codigo jQuery
			$(document).ready(function(){

				// Para boton volver
				$('#btnVolver').click( function() {
			    	window.close();
			    });

				// Boton Imprimir
				$('#btnImprimir').click( function() {
                    window.print();
				});
			});
		</script>
	</body>
</html>
